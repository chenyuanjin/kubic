# 项目级 npm 配置 —— 只作用于 web/,不动全局 ~/.npmrc(那里有你的 npmjs authToken)
#
# 用国内公共镜像 npmmirror(原 cnpm/taobao 源)。
# 实测:registry.npmmirror.com 0.54s vs registry.npmjs.org 6.7s,快约 12 倍。
#
# 与 Maven 同一条纪律:公共镜像 ≠ 公司私服。
# 这里只能出现公开匿名、人人可用的源;任何内网地址或凭据都不允许进这个文件。
registry=https://registry.npmmirror.com
